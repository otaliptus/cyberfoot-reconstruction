import {chromium} from '/Users/talip/.codex/skills/develop-web-game/node_modules/playwright/index.mjs';import assert from 'node:assert/strict';import {mkdirSync} from 'node:fs';
const output='/Users/talip/Documents/ChatGPT/misc/output/native-standings';mkdirSync(output,{recursive:true});const browser=await chromium.launch({headless:true});
try{for(const width of [1024,640]){const page=await browser.newPage({viewport:{width,height:768}}),errors=[];page.on('pageerror',e=>errors.push(String(e)));await page.goto('http://localhost:8766/manager-offer-preview.html?manualClock=1&standingsFixtures=1'+(width===640?'&mode2=1':''));await page.waitForFunction(()=>window.offerDevelopment?.host.active);
 await page.locator('label[for="Form30.r2"]').click();const before=await page.evaluate(()=>Array.from(window.offerDevelopment.save()));
 await page.locator('[data-original-control="Form30.bt2"]').click();await page.waitForFunction(()=>window.offerDevelopment.renderer.frame?.form==='Form36');
 let state=await page.evaluate(()=>JSON.parse(window.render_game_to_text()));assert.ok(state.standings.tables.length>=2);assert.equal(state.finished,false);assert.equal(state.managerClub,11);
 if(width===640){const first=state.standings.tables[0];await page.locator('[data-original-control="Form36.ld2"]').click();await page.waitForFunction(()=>window.offerDevelopment.standings.active?.state.division===3);state=await page.evaluate(()=>JSON.parse(window.render_game_to_text()));assert.deepEqual(state.standings.tables[0],first);assert.equal(state.standings.tables[1].division,3);assert.equal(state.standings.tables[1].rows.length,20);await page.screenshot({path:output+'/division-3.png'});}
 await page.screenshot({path:output+'/league-'+width+'.png'});assert.equal(await page.locator('[data-original-control="Form30.r2"]').isVisible(),false);
 const picker=page.locator('[data-original-control="Form36.combopais"]');const options=await picker.locator('option').count();assert.ok(options>1);await picker.selectOption({index:1});await page.waitForFunction(()=>window.offerDevelopment.standings.active?.state.leagueId===1);
 assert.equal(await page.evaluate(()=>window.offerDevelopment.renderer.frame.standingsData.leagueId),1);await page.screenshot({path:output+'/country-'+width+'.png'});
 if(width===1024)await page.keyboard.press('Escape');else await page.locator('[data-original-control="Form36.Image8"]').click();await page.waitForFunction(()=>window.offerDevelopment.renderer.frame?.form==='Form30');
 state=await page.evaluate(()=>JSON.parse(window.render_game_to_text()));assert.equal(state.offer.selected,1);assert.equal(state.finished,false);assert.deepEqual(await page.evaluate(()=>window.offerDevelopment.runtime.managerJobOffers),[0,1,-1,2]);assert.deepEqual(await page.evaluate(()=>Array.from(window.offerDevelopment.save())),before);
 assert.equal(await picker.isVisible(),false);await page.locator('[data-original-control="Form30.XiButton1"]').click();await page.waitForFunction(()=>JSON.parse(window.render_game_to_text()).finished);assert.deepEqual(errors,[]);await page.close();
}}finally{await browser.close();}
console.log('Standings league screen: original tables, country switch, nested modal, Escape/close, scaled controls, offer preserved and no save mutation verified.');
