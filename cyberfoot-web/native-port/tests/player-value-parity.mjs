import assert from 'node:assert/strict';import fs from 'node:fs';import {originalPlayerValue} from '../player-value.mjs';
const cases=JSON.parse(fs.readFileSync(new URL('./player-value-vectors.json',import.meta.url)));
for(const test of cases){const players=new Uint8Array(304),clubs=new Uint8Array(760),career=new Uint8Array(1888),p=new DataView(players.buffer),c=new DataView(clubs.buffer);for(const [off,key] of [[0x18,'age'],[0x28,'skill'],[0x104,'goals'],[0x30,'talent'],[0x11c,'youthSeason'],[0x50,'field50']])p.setInt32(off,test[key],true);p.setUint8(0x120,test.youth);p.setUint8(0x15,test.star);c.setInt32(0x3c,test.country,true);c.setInt32(0x98,test.strength,true);new DataView(career.buffer).setInt32(0xc0,test.season,true);const save={career,sections:[{name:'players',data:players,recordSize:304,count:1},{name:'clubs',data:clubs,recordSize:760,count:1}]};assert.equal(originalPlayerValue(save,0),test.expected,JSON.stringify(test));}
console.log(`${cases.length} whole original player-value comparisons passed`);
