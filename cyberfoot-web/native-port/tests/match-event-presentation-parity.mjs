import {readFileSync} from 'node:fs';import {verifyMatchEventPresentation} from './match-event-presentation-proof.mjs';const r=verifyMatchEventPresentation(JSON.parse(readFileSync(new URL('./match-event-presentation-vectors.json',import.meta.url))),JSON.parse(readFileSync(new URL('../language.json',import.meta.url))));if(r.failures.length)throw Error(JSON.stringify(r.failures.slice(0,2)));console.log('Original event presentation: '+r.cases+' cases passed.');
