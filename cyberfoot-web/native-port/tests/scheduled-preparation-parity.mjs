import {readFileSync} from 'node:fs';import {verifyScheduledPreparation} from './scheduled-preparation-proof.mjs';const r=verifyScheduledPreparation(JSON.parse(readFileSync(new URL('./scheduled-preparation-vectors.json',import.meta.url))));if(r.failures.length)throw Error(JSON.stringify(r.failures.slice(0,2)));console.log(`Original full silent match preparation: ${r.cases} cases passed.`);
