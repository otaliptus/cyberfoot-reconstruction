import assert from 'node:assert/strict';import fs from 'node:fs';import {restoreNationalAssignments} from '../national-assignment-reset.mjs';
const cases=JSON.parse(fs.readFileSync(new URL('./national-assignment-reset-vectors.json',import.meta.url))),bytes=s=>Uint8Array.from(Buffer.from(s,'hex')),hex=b=>Buffer.from(b).toString('hex');
for(const test of cases){const players=bytes(test.players),clubs=bytes(test.clubs),career=new Uint8Array(1888),c=new DataView(career.buffer),data=new Uint8Array(test.rows.length*8),v=new DataView(data.buffer);test.ids.forEach((n,i)=>c.setInt32(0x604+i*4,n,true));test.rows.forEach((row,i)=>row.forEach((n,j)=>v.setInt32(i*8+j*4,n,true)));const assignments={name:'records_0066b544',data,count:test.rows.length,recordSize:8},save={career,sections:[{name:'players',data:players,recordSize:304,count:12},{name:'clubs',data:clubs,recordSize:760,count:10},assignments]},runtime={nationalManagerCount:test.count,nationalAssignmentsActive:true};restoreNationalAssignments(save,runtime);assert.deepEqual({players:hex(players),clubs:hex(clubs),assignmentsPointer:assignments.count,active:runtime.nationalAssignmentsActive},test.expected);assert.equal(assignments.data.length,0);}
console.log(`${cases.length} original national-assignment restore comparisons passed`);
