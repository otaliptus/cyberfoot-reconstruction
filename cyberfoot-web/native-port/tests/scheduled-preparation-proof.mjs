import {OriginalRandom} from '../match-core.mjs';
import {prepareScheduledMatches} from '../match-preparation.mjs';
const bytes=s=>Uint8Array.from(s.match(/../g)??[],x=>parseInt(x,16)),hex=b=>Array.from(b,n=>n.toString(16).padStart(2,'0')).join('');
export function verifyScheduledPreparation(vectors){const failures=[];for(const [i,v] of vectors.entries()){
 const metadata=new Uint8Array(v.metadata.length*12),m=new DataView(metadata.buffer);v.metadata.flat().forEach((n,j)=>m.setInt32(j*4,n,true));const ref=new Uint8Array(80),refView=new DataView(ref.buffer);v.reference.flat().forEach((n,j)=>refView.setInt32(j*4,n,true));const save={career:bytes(v.career),sections:[{name:'records_0066aca0',recordSize:660,count:v.leagues.length/1320,data:bytes(v.leagues)},{name:'records_0066b608',recordSize:16,count:5,data:ref},{name:'clubs',recordSize:760,count:4,data:bytes(v.clubs)},{name:'records_0066afa0',recordSize:72,count:v.fixtures.length/144,data:bytes(v.fixtures)},{name:'records_0066b6ac',recordSize:12,count:v.metadata.length,data:metadata}]};
 const rng=new OriginalRandom(v.seed),runtime={},result=prepareScheduledMatches(save,v,rng,runtime),raw=new Uint8Array(result.fixtures.length*444),r=new DataView(raw.buffer);
 result.fixtures.forEach((f,j)=>{for(const [o,n] of [[0,f.competition],[4,f.field4],[8,f.clubs[0]],[12,f.clubs[1]],[16,f.lineupIds[0]],[20,f.lineupIds[1]],[24,f.substitutions[0]],[28,f.substitutions[1]],[0x17c,f.savedFixtureId],[0x24,f.income],[0x28,f.field28],...f.attendance.map((n,i)=>[0x2c+i*4,n])])r.setInt32(j*444+o,n,true);});
 const actual={fixtures:hex(raw),stadiums:result.fixtures.map(f=>f.stadium),humanFixtureIds:result.humanFixtureIds,alternateRound:result.alternateRound,timers:[runtime.matchTimerInterval,runtime.batchTimerInterval],seed:rng.state};if(JSON.stringify(actual)!==JSON.stringify(v.expected))failures.push({i,actual,expected:v.expected});
 }return {cases:vectors.length,failures};}
export async function checkScheduledPreparation(){const r=verifyScheduledPreparation(await fetch(new URL('./scheduled-preparation-vectors.json',import.meta.url)).then(r=>r.json()));if(r.failures.length)throw Error(JSON.stringify(r.failures.slice(0,2)));return {...r,failures:0};}
